# mwanachama-backend-forms (Go)

Open tasks only — 🚀 In Progress · 📋 Not Started · ⏸️ Blocked.
Everything else (completed rows, board context) is in [todo_done.md](todo_done.md).

📋 Wire `mwanachama-backend-api-gateway`'s `internal/domain/survey.Repository`/`.RegisterReader` to a new adapter type backed by this package's `FormManager`, the same way actor's eventual gateway wiring is intended to work — not done here, by explicit scope decision (see `CLAUDE.md`).
